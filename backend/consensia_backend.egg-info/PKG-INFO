Metadata-Version: 2.4
Name: consensia-backend
Version: 0.1.0
Summary: FastAPI backend for orchestrating AI personas and judge consensus.
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi<1.0.0,>=0.115.0
Requires-Dist: pydantic<3.0,>=2.9
Requires-Dist: uvicorn[standard]<1.0.0,>=0.30.0
Requires-Dist: httpx<1.0.0,>=0.27.0
Requires-Dist: python-dotenv<2.0.0,>=1.0.0
Requires-Dist: openai<2.0.0,>=1.51.0
Requires-Dist: google-generativeai<1.0.0,>=0.8.2
Provides-Extra: dev
Requires-Dist: ruff<1.0.0,>=0.6.5; extra == "dev"
Requires-Dist: pytest<9.0.0,>=8.3.0; extra == "dev"

# Consensia Backend

FastAPI service that orchestrates persona-level responses and a judge consensus for the Consensia project.

## Getting started

```bash
# Create and activate a virtual environment (example with uv)
uv venv
source .venv/bin/activate

# Install dependencies
uv pip install -r requirements.txt  # or `uv pip install .`
```

Alternatively, using pip:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

### Environment variables

Create a `.env` file in the `backend/` directory and set:

```
# Option A: OpenAI
OPENAI_API_KEY=sk-your-key
OPENAI_MODEL=gpt-4o-mini
JUDGE_MODEL=gpt-4o-mini

# Option B: Gemini (free tier)
GEMINI_API_KEY=AIza...
GEMINI_MODEL=gemini-1.5-flash

# Optional: force provider selection ("openai", "gemini", or leave unset for auto)
LLM_PROVIDER=auto

# Shared config
CORS_ALLOW_ORIGINS=http://localhost:5173
```

### Run the server

```bash
uvicorn app.main:app --reload --port 8000
```

Visit `http://localhost:8000/docs` for interactive API docs.

